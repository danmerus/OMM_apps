import streamlit as st
import numpy as np

st.set_page_config(page_title="OMM – прогноз ССЗРП МХДА", page_icon="🍼")

# ──────────────────────────  HEADER  ──────────────────────────
st.header("OMM ПРОГНОЗ СЗРП МХДА")

st.markdown(
"""
**Цель**  
1. Исходя из данных I-го триместра (11 – 13 ⁶/₇ недель) предсказать вероятность
синдрома селективной задержки роста плода (ССЗРП).  
2. Если риск высок, оценить исход беременности (гибель одного / обоих плодов) на
основании пульсационного индекса (ПИ) в венозном протоке.
"""
)

# ──────────────────────────  INPUTS  ──────────────────────────
st.header("Ввод данных (I-й триместр)")

col1, col2 = st.columns(2)

with col1:
    ktr1 = st.number_input("КТР 1 (мм):", min_value=0.0, step=0.1)
    tvp1 = st.number_input("ТВП 1 (мм):", min_value=0.0, step=0.1)
with col2:
    ktr2 = st.number_input("КТР 2 (мм):", min_value=0.0, step=0.1)
    tvp2 = st.number_input("ТВП 2 (мм):", min_value=0.0, step=0.1)

tkr = st.radio(
    "Трикуспидальная регургитация (ТКР) хотя бы у одного плода:",
    options=[0, 1],
    format_func=lambda x: "Есть" if x == 1 else "Нет",
    horizontal=True,
)

# ──────────────────────────  STAGE 1  ──────────────────────────
st.subheader("1-й этап → риск ССЗРП")

# Линейная комбинация
Z = (
    2.75
    + 0.20 * (ktr1 - ktr2)
    + 0.71 * (tvp1 - tvp2)
    - 3.75 * min(tvp1, tvp2)
    + 2.27 * tkr
)

# Логистическая функция
prob = 1 / (1 + np.exp(-Z))

# Вывод результата
st.metric("Вероятность ССЗРП", f"{prob*100:.1f} %")

if prob > 0.15:
    st.error("⚠️ Высокий риск ССЗРП")
else:
    st.success("Низкий риск ССЗРП")

# ──────────────────────────  STAGE 2  ──────────────────────────
st.subheader("2-й этап → прогноз исхода (только если риск высок)")

if prob > 0.15:
    pi = st.number_input(
        "Максимальный ПИ в венозном протоке (у плодов):", min_value=0.0, step=0.01
    )

    if pi > 1.3:
        st.error(
            "Прогноз: вероятна гибель одного/двух плодов.\n"
            "👉 Рассмотрите коагуляцию сосудов пуповины меньшего плода."
        )
    else:
        st.success(
            "Прогноз: оба плода, вероятно, выживут.\n"
            "👉 Рекомендуется динамическое наблюдение."
        )
else:
    st.info("Введите показатели ПИ, если риск ССЗРП оказался высоким выше.")
